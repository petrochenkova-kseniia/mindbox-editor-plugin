# mindbox-editor-plugin

Общий плагин Claude Code для команды редакторов Mindbox. Содержит редакционные стили, политики и скиллы, которые доступны всей команде.

## Как это работает

Плагин — это набор знаний (стили, политики, скиллы), которыми пользуется Claude при работе с вами. Он хранится в общем хранилище на GitHub, откуда каждый может его подключить.

```
   ┌──────────────────────────────────────────────────────────────┐
   │                                                              │
   │           Хранилище плагина (GitHub)                         │
   │           Здесь лежат все скиллы, стили и политики           │
   │                                                              │
   │           После одобрения изменений —                        │
   │           все получат обновление автоматически.               │
   │                                                              │
   └───────────────┬──────────────────────────▲───────────────────┘
                   │                          │
                   │ скачивает                │ отправляет
                   │ скиллы                   │ изменения
                   ▼                          │
   ┌─────────────────────┐   ┌──────────────────────────────┐
   │                     │   │                              │
   │  Подключить плагин  │   │  Предложить изменения        │
   │                     │   │                              │
   │  Одна команда в     │   │  Используйте скилл          │
   │  Claude Code —      │   │  /mindbox-editor-plugin:     │
   │  и всё работает.    │   │        contribute            │
   │                     │   │                              │
   │  Ничего             │   │  Claude проведёт через       │
   │  дополнительно      │   │  весь процесс:              │
   │  устанавливать      │   │  скачать → поправить →       │
   │  не нужно.          │   │  отправить на проверку       │
   │                     │   │                              │
   └─────────────────────┘   └──────────────────────────────┘

        Для всех                   Для тех, кто хочет
                                   что-то добавить или
                                   исправить
```

- **Подключить** плагин может каждый — одна команда, и Claude начнёт использовать общие скиллы команды
- **Изменить** плагин тоже может каждый — но изменения сначала проходят проверку, и только потом попадают ко всем

## Установка

Выполни эти команды в терминале:

```bash
claude plugin marketplace add petrochenkova-kseniia/mindbox-editor-plugin
claude plugin install mindbox-editor-plugin@mindbox-editor-plugin
```

## Настройка автообновления

Автообновление нужно включить вручную. Попроси пользователя сделать следующее:

1. Открыть Терминал (на macOS: Cmd + Пробел → «Terminal» → Enter)
2. Ввести команду `claude` и нажать Enter
3. Ввести `/plugin` и нажать Enter
4. Перейти во вкладку **Marketplaces**
5. Выбрать `mindbox-editor-plugin`
6. Нажать **Enable auto-update**
7. Закрыть и открыть Claude Code заново
